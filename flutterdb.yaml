server:
  addr: "0.0.0.0"
  port: 1234

cluster:
  #node-name: "andryushka"                  # defaults to hostname
  membership-listen-addr: "127.0.0.1"       # cluster membership listener addr
  membership-listen-port: 5678              # cluster membership listener port
  membership-join-addrs: []                 # addresses of cluster nodes to join

metastore:
  data-dir: "/Users/andrew/src/andrew/flutterdb/testdata/metastore"

client:
  remote-addr: "127.0.0.1"
  remote-port: 1234
